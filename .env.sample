# Google Apps Script Web App URL that serves the audience comments feed.
# Deploy gas/Code.gs as a Web App and paste its /exec URL here.
# This URL is a bearer secret: copy this file to .env (gitignored) and fill it in.
# When unset, the presenter still works; the comment feed is simply empty.
COMMENTS_FEED_URL=
